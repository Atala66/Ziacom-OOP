/*import { Routes, RouterModule } from '@angular/router';

import {UsersBaseComponent} from './users-base.component';
import {UsersFormComponent} from "./users-form/users-form.component";


// el crud de users lo vamos a enrutar internamente
const usersRoutes: Routes = [
  { path: 'users', component: UsersBaseComponent},
    { path: 'users/new', component: UsersFormComponent}, 
  { path: 'users/newUser', component: UsersFormComponent}
];
export const usersRouting = RouterModule.forChild(usersRoutes); */
